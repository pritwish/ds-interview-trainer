workflows:
  ios-build:
    name: DS Interviewer - iOS Build
    max_build_duration: 30
    environment:
      xcode: 16.2

    scripts:
      - name: Install xcodegen
        script: brew install xcodegen

      - name: Generate Xcode project
        script: xcodegen generate

      - name: Build for simulator
        script: |
          set -o pipefail
          xcodebuild \
            -project DSInterviewer.xcodeproj \
            -scheme DSInterviewer \
            -sdk iphonesimulator \
            -destination 'generic/platform=iOS Simulator' \
            -configuration Debug \
            -derivedDataPath build \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            build 2>&1 | tee /tmp/build.log | grep -E "error:|warning: |Build succeeded|BUILD FAILED" || true
          echo "--- ERRORS ---"
          grep "error:" /tmp/build.log || echo "No error lines found"
          grep -c "error:" /tmp/build.log | xargs -I{} test {} -eq 0 || exit 1

    artifacts:
      - build/Build/Products/Debug-iphonesimulator/DSInterviewer.app
